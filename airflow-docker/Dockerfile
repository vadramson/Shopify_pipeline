# Use the official Apache Airflow image as a base
FROM apache/airflow:2.7.2-python3.9

# Install the Docker provider package
RUN pip install apache-airflow-providers-docker

# Copy any additional configuration files if needed
# COPY airflow.cfg /opt/airflow/airflow.cfg

# Expose ports or other setup if needed
# EXPOSE 8080
